const paragraph = document.getElementById("paragraph");
      paragraph.style.color = "red";

const pageTitle = document.getElementById("page-title");
      pageTitle.innerText = "A lagoa Azul";

const secondParagraph = document.getElementById("second-paragraph");
      secondParagraph.style.background = "green";

const subtitle = document.getElementById("subtitle");
      subtitle.style.color = "pink";

const paragrafos = document.getElementsByClassName('paragrafos');
      paragrafos[0].style.background = "yellow"
      // for (let i = 0; i < paragrafos.length; i+= 1) {
      // paragrafos[i].style.background = "yellow";
      // }

const subtitulo = document.getElementsByTagName('h4');
      subtitulo[0].innerText = "Esta frase esta na cor rosa"

